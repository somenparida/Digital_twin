apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: campus-frontend
spec:
  type: NodePort
  selector:
    app: campus-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
